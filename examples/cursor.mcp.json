{
  "mcpServers": {
    "flux-kontext": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/flux-kontext-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
